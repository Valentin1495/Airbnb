"use client";

import { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { wrap } from "popmotion";
import { ChevronLeftIcon, ChevronRightIcon } from "@heroicons/react/24/outline";

export default function Image({ images }: { images: string[] }) {
  return (
    <>
      {/* <button className="next" onClick={() => paginate(1)}>
        <ChevronRightIcon className="h-8 w-8 bg-white/80 rounded-full p-1.5" />
      </button>
      <button className="prev" onClick={() => paginate(-1)}>
        <ChevronLeftIcon className="h-8 w-8 bg-gray-200 rounded-full p-1.5" />
      </button> */}
    </>
  );
}
